import Link from 'next/link';
import {notFound} from 'next/navigation';
import {getActor} from '@/lib/auth';
import {getPublicData,getDashboardData,getOrderData,getServiceData,getCreatorData,getRequestData,getAuctionData} from '@/lib/read-model';
import {Badge,CommandForm,Empty,Field,OrderList,ServiceCard,date,money,num,row,rows,str} from '@/components/ui';
import {mockPaymentsEnabled} from '@/modules/payments/funding';
import {listInAppNotifications} from '@/modules/notifications/store';
export const dynamic='force-dynamic';
type Props={params:Promise<{path?:string[]}>,searchParams:Promise<Record<string,string|string[]|undefined>>};
const categories=['CREATE','PUBLISH','ACCESS','DIGITAL'];
function Notices({query}:{query:Record<string,string|string[]|undefined>}){return <>{query.error&&<div role="alert" className="notice error">{String(query.error)} <Link href="/support">Get help →</Link></div>}{query.message&&<div role="status" className="notice success">{String(query.message)}</div>}</>}
function CategoryField(){return <Field name="taxonomy" label="What are you offering?"><select name="taxonomy">{categories.map(c=><option key={c}>{c}</option>)}</select></Field>}
function RequestCard({item}: {item:unknown}){const r=row(item);return <Link className="panel" href={`/requests/${str(r.id)}`}><div className="inline-actions"><Badge>{str(r.taxonomy)}</Badge><span className="muted">{str(r.status)}</span></div><h3>{str(r.title)}</h3><p>{str(r.brief).slice(0,180)}</p><div className="service-bottom"><strong>{money(r.budget_minor)} budget</strong><span>{num(r.target_hires)} creator{num(r.target_hires)!==1?'s':''} · {num(r.application_count)} applications</span></div></Link>}
function AuctionCard({item}:{item:unknown}){const a=row(item);return <Link className="panel" href={`/auctions/${str(a.id)}`}><Badge>{str(a.status)}</Badge><h3>{str(a.title)}</h3><p>By {str(a.creator_name)} · Ends {date(a.ends_at)}</p><div className="service-bottom"><strong>{money(a.current_price_minor || a.starting_price_minor)}</strong><span>{num(a.bid_count)} bids · View auction ↗</span></div></Link>}
function PageHeading({eyebrow,title,description}:{eyebrow:string,title:string,description?:string}){return <div className="page-heading"><div className="eyebrow">{eyebrow}</div><h1>{title}</h1>{description&&<p>{description}</p>}</div>}
export default async function Page({params,searchParams}:Props){const [{path=[]},query,actor]=await Promise.all([params,searchParams,getActor()]);const route='/'+path.join('/');const notices=<Notices query={query}/>;
if(route==='/' || route==='/explore' || route==='/requests' || route==='/auctions'){
const data=await getPublicData({q:typeof query.q==='string'?query.q:undefined,category:typeof query.category==='string'?query.category:undefined});const services=rows(data.services);
return <main className="container">{notices}{route==='/'?<><section className="hero"><div><div className="eyebrow"><span className="dot"/>Independent talent. Open for good work.</div><h1>Your next big idea.<br/><em>The right creator.</em></h1><p>Find people who get your vision. Book their next available slot, agree on the details, and make something worth sharing.</p><div className="hero-actions"><Link href="/explore" className="button button-dark">Find your creator <span>↗</span></Link><Link href="/sign-up?role=creator" className="button button-outline">Offer your skills</Link></div><span className="hero-note">Clear scope. Real availability. 0% platform fees.</span></div><div className="hero-art" aria-label="Creators keep 100% before third-party payment costs"><div className="hero-ring"/><div className="art-tag">Built around your craft ↗</div><div className="floating-card"><div className="eyebrow">More room to create</div><h3>Your work.<br/>Your rate.<br/>Your whole payment.</h3><div className="fee"><span>Platform fee</span><strong>0%</strong></div><p className="muted">Third-party costs, if any, are disclosed separately.</p></div><div className="big-star" aria-hidden>✳</div></div></section><div className="value-strip"><div><strong>01 / Find the right fit</strong><p>Browse work samples and clear service scopes.</p></div><div><strong>02 / Book actual availability</strong><p>Reserve a creator’s available capacity.</p></div><div><strong>03 / Make it happen</strong><p>One shared space for briefs, delivery, and approval.</p></div></div><div className="section-heading"><div><div className="eyebrow">Ready for your next project</div><h2>Good people. Great possibilities.</h2></div><Link className="text-link" href="/explore">Explore all ↗</Link></div>{services.length?<div className="service-grid">{services.slice(0,6).map((s,i)=><ServiceCard key={str(s.id)} service={s} index={i}/>)}</div>:<Empty title="The marketplace is ready for its first creators">Create a service with work samples and real availability to get started.</Empty>}</>:route==='/explore'?<><PageHeading eyebrow="The marketplace" title="Find your kind of creative." description="Explore clear scopes, work samples, and creators with room for your project."/><form method="get" className="filters"><input aria-label="Search services" name="q" placeholder="Try launch writing, video, design…" defaultValue={str(query.q)}/><select aria-label="Category" name="category" defaultValue={str(query.category)}><option value="">All categories</option>{categories.map(c=><option key={c}>{c}</option>)}</select><button className="button button-dark">Search ↗</button></form><p className="muted">{services.length} service{services.length!==1?'s':''} found · all with 0% platform fees</p>{services.length?<div className="service-grid">{services.map((s,i)=><ServiceCard key={str(s.id)} service={s} index={i}/>)}</div>:<Empty title="No matching services">Try another search or come back as creators publish new work.</Empty>}</>:route==='/requests'?<><div className="section-heading"><PageHeading eyebrow="Open briefs" title="Bring your next project to life." description="Share what you need. Creators bring their approach, samples, and quote."/><Link className="button button-dark" href="/buyer/requests/new">Post a brief ↗</Link></div><div className="cards">{rows(data.requests).length?rows(data.requests).map(r=><RequestCard key={str(r.id)} item={r}/>):<Empty title="No open briefs yet">Be the first to share a project with the creator community.</Empty>}</div></>:<><PageHeading eyebrow="Limited availability" title="A great slot. Your best offer." description="Bid on creator availability. All deadlines are shown in UTC; accepted bids are confirmed by the server."/><div className="service-grid">{rows(data.auctions).length?rows(data.auctions).map(a=><AuctionCard key={str(a.id)} item={a}/>):<Empty title="No auctions open right now">Explore services you can book directly while new auctions are prepared.</Empty>}</div></>}</main>}
if(route==='/sign-in'||route==='/sign-up'){const signup=route==='/sign-up';return <main className="auth-layout"><div><div className="eyebrow">A little less friction. A lot more possibility.</div><h1>{signup?'Good work starts with a hello.':'Welcome back to your next idea.'}</h1><p>One account to hire creative people, offer your skills, and keep your projects moving.</p><div className="fee-note">This is a local sandbox. Create a test account using an email and password. No email is sent and no real payments are processed.</div></div><div className="panel">{notices}<h2>{signup?'Create your account':'Log in to Capacity'}</h2><form action="/api/auth" method="post"><input type="hidden" name="action" value={signup?'signup':'login'}/><input type="hidden" name="return_to" value={typeof query.return_to==='string'?query.return_to:'/dashboard'}/>{signup&&<Field name="display_name" label="Your name" required/>}<Field name="email" type="email" label="Email address" required/><Field name="password" type="password" label="Password (minimum 12 characters)" required/>{signup&&<Field name="role" label="What brings you here?"><select name="role" defaultValue={str(query.role,'buyer')}><option value="buyer">I want to hire creators</option><option value="creator">I want to offer my skills</option></select></Field>}<button className="button button-dark">{signup?'Create account':'Log in'} ↗</button></form><p className="muted">{signup?'Already have an account?':'New around here?'} <Link className="text-link" href={signup?'/sign-in':'/sign-up'}>{signup?'Log in':'Create an account'}</Link></p></div></main>}
if(path[0]==='services'&&path[1]){const result=await getServiceData(path[1]);if(!result)notFound();const d=row(result),s=row(d.service),c=row(d.creator);return <main className="container">{notices}<div className="breadcrumbs"><Link href="/explore">Explore</Link> / {str(s.taxonomy)} / {str(s.title)}</div><div className="split"><div><div className="detail-hero"><Badge>{str(s.taxonomy)}</Badge><h1>{str(s.title)}</h1><Link className="creator-line" href={`/creators/${str(c.handle)}`}><span className="avatar small">{str(c.display_name,'C')[0]}</span>By {str(c.display_name ?? s.creator_name)} ↗</Link></div><div className="panel"><h2>The work, clearly defined.</h2><p className="prewrap">{str(s.description)}</p><h3>Usage & publishing</h3><p>{str(s.taxonomy)==='PUBLISH'?'This service involves publishing on the creator’s channel. Confirm channel, disclosure, and posting scope in the brief.':str(s.taxonomy)==='ACCESS'?'You are booking access to the creator’s expertise. Agree on the meeting schedule in the brief.':'Content is delivered for the buyer to use. Posting to the creator’s channel is not included unless explicitly agreed in the scope.'}</p></div><div className="panel"><h2>A look at their work</h2>{rows(d.samples).length?rows(d.samples).map(sample=><div className="record" key={str(sample.id ?? sample.url)}><a href={str(sample.url)} target="_blank" rel="noreferrer" className="text-link">{str(sample.title,'View sample')} ↗</a><p>{str(sample.description)}</p></div>):<p>No linked work samples.</p>}</div></div><aside><div className="panel"><div className="eyebrow">One clear price</div><div className="price-big">{money(s.price_minor)}</div><ul className="facts"><li><span>Delivery from complete brief</span><strong>{num(s.turnaround_hours)} hours</strong></li><li><span>Included revisions</span><strong>{num(s.revision_limit)}</strong></li><li><span>Available capacity</span><strong>{num(s.available_units)} / {num(s.total_units)}</strong></li><li><span>Platform fee</span><strong>$0.00</strong></li></ul>{actor?num(s.available_units)>0?<CommandForm command="book" label="Reserve this service" values={{service_id:str(s.id)}}><Field name="brief" label="Tell the creator about your project" type="textarea" required placeholder="Your product, audience, goals, links, and requirements (at least 20 characters)."/></CommandForm>:<Empty title="Fully booked">Check another creator or post an open brief.</Empty>:<Link className="button button-dark" href={`/sign-in?return_to=${encodeURIComponent(route)}`}>Log in to book ↗</Link>}<div className="fee-note">Local sandbox: checkout uses simulated funds. Your reservation is time limited. Delivery begins after funding and a complete brief.</div></div></aside></div></main>}
if(path[0]==='creators'&&path[1]){const result=await getCreatorData(path[1]);if(!result)notFound();const d=row(result),c=row(d.creator);return <main className="container">{notices}<div className="panel"><div className="avatar">{str(c.display_name,'C')[0]}</div><PageHeading eyebrow={str(c.niche,'Independent creator')} title={str(c.display_name)} description={str(c.bio)}/><div className="inline-actions"><Badge>{num(c.completed_jobs)} completed jobs</Badge>{c.rating!=null&&<Badge>{str(c.rating)} rating</Badge>}</div></div><div className="section-heading"><h2>Ways to work together</h2></div><div className="service-grid">{rows(d.services).map((s,i)=><ServiceCard key={str(s.id)} service={s} index={i}/>)}</div><div className="section-heading"><h2>Work samples</h2></div><div className="panel">{rows(d.samples).length?rows(d.samples).map(s=><div className="record" key={str(s.url)}><a className="text-link" href={str(s.url)} target="_blank" rel="noreferrer">{str(s.title)} ↗</a><p>{str(s.description)}</p></div>):<Empty title="No samples yet"/>}</div></main>}
if(path[0]==='requests'&&path[1]){const result=await getRequestData(actor,path[1]);if(!result)notFound();const d=row(result),r=row(d.request),owner=actor?.id===str(r.buyer_id);return <main className="container">{notices}<Link href="/requests" className="breadcrumbs">← All open briefs</Link><PageHeading eyebrow={str(r.taxonomy)} title={str(r.title)} description={`Posted by ${str(r.buyer_name)} · ${str(r.status)}`}/><div className="split"><div><div className="panel"><h2>The brief</h2><p className="prewrap">{str(r.brief)}</p><ul className="facts"><li>Budget<strong>{money(r.budget_minor)}</strong></li><li>Creators needed<strong>{num(r.target_hires)}</strong></li><li>Deadline<strong>{date(r.deadline)}</strong></li></ul></div><div className="panel"><h2>{owner?'Compare applications':'Your applications'}</h2>{rows(d.applications).length?rows(d.applications).map(a=><div className="record" key={str(a.id)}><div className="inline-actions"><h3>{str(a.creator_name)}</h3><Badge>{str(a.status)}</Badge></div><p>{str(a.note)}</p><p><strong>{money(a.quote_minor)}</strong></p>{owner&&str(a.status)==='SUBMITTED'&&<CommandForm command="select_application" label="Select creator" values={{application_id:str(a.id)}} returnTo={route}/>} {!owner&&str(a.status)==='SELECTED'&&<><CommandForm command="accept_offer" label="Accept offer" values={{application_id:str(a.id)}}/><CommandForm command="decline_offer" label="Decline offer" values={{application_id:str(a.id)}} returnTo={route}/></>}</div>):<Empty title="No applications to show">Applications are visible to the buyer and their respective creators.</Empty>}</div></div><aside className="panel"><h2>{owner?'Your brief is live':'Bring your approach'}</h2>{!owner&&actor?<CommandForm command="apply" label="Send application" values={{request_id:str(r.id)}} returnTo={route}><Field name="quote" label="Your quote (USD)" type="number" required/><Field name="turnaround_hours" label="Delivery time (hours)" type="number" value="48" required/><Field name="note" label="Your approach and relevant samples" type="textarea" required/></CommandForm>:!actor?<Link className="button button-dark" href="/sign-in">Log in to apply ↗</Link>:<p>Review the work samples and scope before selecting a creator. The lowest quote does not automatically win.</p>}</aside></div></main>}
if(path[0]==='auctions'&&path[1]){const result=await getAuctionData(actor,path[1]);if(!result)notFound();const d=row(result),a=row(d.auction),minimum=num(a.current_price_minor)?num(a.current_price_minor)+num(a.minimum_increment_minor):num(a.starting_price_minor);return <main className="container">{notices}<PageHeading eyebrow={`Auction · ${str(a.status)}`} title={str(a.title)} description={`By ${str(a.creator_name)} · Server deadline: ${date(a.ends_at)}`}/><div className="split"><div><div className="panel"><h2>Current standing</h2><div className="price-big">{money(a.current_price_minor || a.starting_price_minor)}</div><p>{num(a.bid_count)} accepted bids · Minimum next bid {money(minimum)}</p><Link className="text-link" href={`/services/${str(a.service_id)}`}>Read the full service scope and samples ↗</Link><p className="muted">Refresh to see the latest server-confirmed bids. A submitted form is not an accepted bid until the server confirms it. Buy Now is available only before the first valid bid.</p></div><div className="panel"><h2>Bid history</h2>{rows(d.bids).length?rows(d.bids).map((b,i)=><div className="record" key={i}><strong>{money(b.amount_minor)}</strong><p>{str(b.display_name,'Bidder')} · {date(b.created_at)}</p></div>):<Empty title="Be the first to bid"/>}</div></div><aside className="panel"><h2>Make your offer</h2>{actor&&str(a.status)==='LIVE'?<><CommandForm command="bid" label="Place binding bid" values={{auction_id:str(a.id)}} returnTo={route}><Field name="amount" label="Bid amount (USD)" type="number" value={(minimum/100).toFixed(2)} required/></CommandForm>{num(a.bid_count)===0&&num(a.buy_now_price_minor)>0&&<CommandForm command="buy_now" label={`Buy now · ${money(a.buy_now_price_minor)}`} values={{auction_id:str(a.id)}}/>}</>:!actor?<Link className="button button-dark" href="/sign-in">Log in to bid ↗</Link>:<p>This auction is not accepting bids.</p>}{actor?.id===str(a.seller_id)&&<CommandForm command="close_auction" label="Close after deadline" values={{auction_id:str(a.id)}} returnTo={route}/>}<div className="fee-note">Local sandbox only. Winning bids create an order requiring simulated checkout. No real funds are involved.</div></aside></div></main>}
if(['terms','privacy','refund-policy','support','reset-password'].includes(path[0]||'')){return <main className="container"><PageHeading eyebrow="Local sandbox information" title={route==='/support'?'How can we help?':route==='/privacy'?'Your project stays yours.':route==='/refund-policy'?'Refunds & revisions':route==='/reset-password'?'Account recovery':'Working together on Capacity'}/><div className="panel">{notices}{route==='/support'?<><h2>Get help with an order</h2><p>Open your order workspace to send a message or raise a dispute. This local environment has no external support inbox and sends no email.</p><Link className="button button-dark" href="/dashboard">Open your workspace ↗</Link></>:route==='/reset-password'?<p>Email recovery requires a configured authentication email provider. It is not enabled in this local sandbox. Use a separate test account to continue local acceptance testing.</p>:<><p>This environment is for local product testing. These are product operating rules, not reviewed production legal terms.</p><h3>Clear scope and zero platform fees</h3><p>Review the service scope, delivery period, included revisions, and amount before booking. The platform fee is always 0%. Third-party payment costs are separate and must be disclosed before any live transaction.</p><h3>Delivery, review, and refunds</h3><p>Buyer and creator work inside the order workspace. Buyers may request an included revision, approve submitted work, or raise a dispute. Refunds follow authorized order transitions; a cancellation is not proof of a completed refund.</p><h3>Privacy</h3><p>Private briefs, messages, and deliveries are visible only to authorized order participants and scoped support roles. Public services and profile samples are visible to visitors. Do not use real confidential or payment data in local tests.</p><div className="notice">Production launch is blocked until final policies, support contact, privacy controls, and payment capabilities are reviewed and configured.</div></>}</div></main>}
if(!actor)return <main className="container">{notices}<Empty title="Your workspace is one login away"><Link className="button button-dark" href={`/sign-in?return_to=${encodeURIComponent(route)}`}>Log in to continue ↗</Link></Empty></main>;
if(route==='/dashboard'){
  const [d,notifications]=[row(await getDashboardData(actor)),await listInAppNotifications(actor.id,8)];
  const stats=row(d.stats);
  const orders=rows(d.orders);
  const active=orders.filter(o=>!['COMPLETED','CANCELLED','REFUNDED'].includes(str(o.status)));
  return <main className="container"><div className="workspace"><aside className="sidebar"><div className="sidebar-profile"><div className="avatar">{str(actor.display_name,'C')[0]}</div><strong>{actor.display_name}</strong><span className="muted">{actor.email}</span></div><h4>Workspace</h4><Link className="active" href="/dashboard">Overview</Link><Link href="/creator/services">My services</Link><Link href="/buyer/orders">Orders</Link><Link href="/buyer/requests">Open briefs</Link><h4>Create</h4><Link href="/creator/services/new">New service</Link><Link href="/buyer/requests/new">Post a brief</Link><Link href="/creator/auctions/new">New auction</Link><h4>Account</h4><Link href="/settings/profile">Profile</Link></aside><section className="workspace-main">{notices}<div className="page-heading"><div className="eyebrow">Your workspace</div><h1>Keep good work moving.</h1><p>Track orders, publish capacity, and respond to opportunities from one place.</p></div><div className="stats"><div className="stat"><span>Completed orders</span><strong>{num(stats.completed_orders)}</strong></div><div className="stat"><span>Active orders</span><strong>{num(stats.active_orders)}</strong></div><div className="stat"><span>Gross volume</span><strong>{money(stats.gross_minor)}</strong></div><div className="stat"><span>Available capacity</span><strong>{num(stats.available_minor)}</strong></div><div className="stat"><span>Platform fee</span><strong>$0.00</strong></div></div><div className="section-heading"><h2>Notifications</h2></div>{notifications.length?<div className="panel">{notifications.map(n=><Link className="record" key={n.id} href={n.link_path}><strong>{n.subject}</strong><p>{n.body}</p><span className="muted">{date(n.created_at)}</span></Link>)}</div>:<p className="muted">No notifications yet.</p>}<div className="section-heading"><h2>Recent orders</h2><Link className="text-link" href="/buyer/orders">View all ↗</Link></div>{orders.length?<OrderList orders={orders.slice(0,8)}/>:<Empty title="Your next collaboration starts here"><Link href="/explore" className="text-link">Find a creator →</Link></Empty>}<div className="section-heading"><h2>Shortcuts</h2></div><div className="service-grid"><Link className="panel" href="/creator/services/new"><Badge>Creator</Badge><h3>Publish a service</h3><p>Set a clear scope, price, samples, and real available capacity.</p></Link><Link className="panel" href="/buyer/requests/new"><Badge>Buyer</Badge><h3>Post an open brief</h3><p>Tell the community what you need and compare approaches.</p></Link><Link className="panel" href="/creator/auctions/new"><Badge>Optional</Badge><h3>Open an auction</h3><p>Offer a time-bound slot when direct booking is not the right fit.</p></Link></div></section></div></main>;
}
if(path[0]==='orders'&&path[1]){
  const result=await getOrderData(actor,path[1]);
  if(!result)notFound();
  const d=row(result),o=row(d.order),delivery=rows(d.deliveries),events=rows(d.events),messages=rows(d.messages),reviews=rows(d.reviews);
  const creator=actor.id===str(o.creator_id),buyer=actor.id===str(o.buyer_id);
  return <main className="container">{notices}<div className="breadcrumbs"><Link href="/dashboard">Workspace</Link> / Order {str(o.id).slice(0,8)}</div><div className="section-heading"><PageHeading eyebrow={`${str(o.source,'BOOK')} order · ${str(o.status)}`} title={str(o.title)} description={`${str(o.buyer_name)} and ${str(o.creator_name)} · ${money(o.amount_minor)} · Platform fee $0.00`}/><Badge>{str(o.payment_status,'PENDING')}</Badge></div><div className="split"><div><div className="panel"><h2>Project brief</h2><p className="prewrap">{str(o.brief)}</p><ul className="facts"><li><span>Delivery due</span><strong>{date(o.delivery_due_at)}</strong></li><li><span>Review deadline</span><strong>{date(o.review_due_at)}</strong></li><li><span>Included revisions</span><strong>{num(o.revision_count)} / 1 used</strong></li><li><span>Settlement</span><strong>{str(o.settlement_status,'NOT_READY')}</strong></li></ul></div><div className="panel"><h2>Delivery</h2>{delivery.length?delivery.map(item=><div className="record" key={str(item.id)}><div className="inline-actions"><Badge>Version {str(item.version)}</Badge><span className="muted">{date(item.created_at)}</span></div><p className="prewrap">{str(item.body)}</p>{Boolean(item.url)&&<a className="text-link" href={str(item.url)} target="_blank" rel="noreferrer">Open attachment ↗</a>}</div>):<p className="muted">The creator has not delivered work yet.</p>}{creator&&['FUNDED','IN_PROGRESS','REVISION_REQUESTED'].includes(str(o.status))&&<CommandForm command="deliver" label="Submit delivery" values={{order_id:str(o.id)}} returnTo={route}><Field name="body" label="Delivery note" type="textarea" required placeholder="Explain what is ready, where to find it, and any usage notes."/><Field name="url" label="Optional file or link" placeholder="https://…"/></CommandForm>}</div><div className="panel"><h2>Timeline</h2>{events.length?<div className="timeline">{events.map(event=><div className="timeline-item" key={str(event.id)}><strong>{str(event.kind).replaceAll('_',' ')}</strong><p>{date(event.created_at)}</p></div>)}</div>:<p className="muted">No events yet.</p>}</div></div><aside><div className="panel"><h2>Next step</h2>{str(o.status)==='AWAITING_PAYMENT'&&buyer&&mockPaymentsEnabled()&&<form method="post" action="/api/dev/mock-checkout" className="command-form"><input type="hidden" name="order_id" value={str(o.id)}/><p className="muted">Local test provider. The order is funded only after the provider&apos;s signed confirmation is verified. No real funds move.</p><button className="button" type="submit">Pay with local test provider<span aria-hidden>↗</span></button></form>}{str(o.status)==='FUNDED'&&creator&&<CommandForm command="start" label="Start work" values={{order_id:str(o.id)}} returnTo={route}/>} {str(o.status)==='DELIVERED'&&buyer&&<><CommandForm command="approve" label="Approve delivery" values={{order_id:str(o.id)}} returnTo={route}/><CommandForm command="revision" label="Request included revision" values={{order_id:str(o.id)}} returnTo={route}><Field name="body" label="What should change?" type="textarea" required/></CommandForm><CommandForm command="dispute" label="Open a dispute" values={{order_id:str(o.id)}} returnTo={route}><Field name="body" label="Reason" type="textarea" required/></CommandForm></>}{str(o.status)==='REVISION_REQUESTED'&&creator&&<p>Submit the updated delivery above. The order remains in the workspace until the buyer approves it.</p>}{['AWAITING_PAYMENT','FUNDED'].includes(str(o.status))&&(buyer||creator)&&<CommandForm command="cancel" label="Cancel order" values={{order_id:str(o.id)}} returnTo={route}/>} {['APPROVED','COMPLETED'].includes(str(o.status))&&buyer&&reviews.length===0&&<CommandForm command="review" label="Leave a review" values={{order_id:str(o.id)}} returnTo={route}><Field name="rating" label="Rating (1–5)" type="number" value="5" required/><Field name="body" label="What stood out?" type="textarea" required/></CommandForm>}{str(o.status)==='CANCELLED'&&str(o.payment_status)==='REFUND_PENDING'&&buyer&&<CommandForm command="refund" label="Check refund with provider" values={{order_id:str(o.id)}} returnTo={route}><p className="muted">A full refund was requested. It shows as refunded only after the provider confirms.</p></CommandForm>}<div className="fee-note">All state changes are server-authorized and versioned. The platform fee is 0%; any provider cost is tracked separately.</div></div><div className="panel"><h2>Messages</h2>{messages.map(message=><div className="record" key={str(message.id)}><strong>{str(message.display_name)}</strong><p>{str(message.body)}</p><span className="muted">{date(message.created_at)}</span></div>)}<CommandForm command="message" label="Send message" values={{order_id:str(o.id)}} returnTo={route}><Field name="body" label="Message" type="textarea" required placeholder="Keep project details in this private workspace."/></CommandForm></div></aside></div></main>;
}
if(route==='/creator/services/new')return <main className="container">{notices}<PageHeading eyebrow="Creator setup" title="Offer a clear next step." description="A service needs a real scope, price, samples, and capacity before it can be published."/><div className="panel"><CommandForm command="create_service" label="Save draft service" returnTo="/creator/services"><div className="form-grid"><Field name="title" label="Service title" required placeholder="Launch story and landing page copy"/><CategoryField/><Field name="price" label="Price (USD)" type="number" required placeholder="500"/><Field name="capacity" label="Available slots" type="number" value="3" required/><Field name="turnaround_hours" label="Delivery time (hours)" type="number" value="72" required/><Field name="niche" label="Niche or specialty" placeholder="Brand strategy, video, design…"/></div><Field name="description" label="Scope and deliverables" type="textarea" required placeholder="What the buyer receives, what is out of scope, and what a complete brief includes."/><h3>Work samples</h3><div className="form-grid"><Field name="sample_url_1" label="Sample URL 1" required/><Field name="sample_title_1" label="Sample title 1" required/><Field name="sample_url_2" label="Sample URL 2" required/><Field name="sample_title_2" label="Sample title 2" required/><Field name="sample_url_3" label="Sample URL 3" required/><Field name="sample_title_3" label="Sample title 3" required/></div><p className="muted">The draft is created first. Publishing is a separate action so you can review the final scope.</p></CommandForm></div></main>;
if(route==='/creator/services'){
  const d=row(await getDashboardData(actor));
  return <main className="container">{notices}<div className="section-heading"><PageHeading eyebrow="Creator workspace" title="My services" description="Publish only services with complete scope, samples, and available capacity."/><Link className="button button-dark" href="/creator/services/new">New service ↗</Link></div>{rows(d.services).length?<div className="cards">{rows(d.services).map(s=><div className="panel" key={str(s.id)}><div className="inline-actions"><Badge>{str(s.status)}</Badge><span className="muted">{num(s.available_units)} / {num(s.total_units)} slots</span></div><h3>{str(s.title)}</h3><p>{str(s.description)}</p><div className="service-bottom"><strong>{money(s.price_minor)}</strong><span>{num(s.turnaround_hours)} hours · {str(s.taxonomy)}</span></div><div className="inline-actions"><Link className="text-link" href={`/services/${str(s.id)}`}>Open public page ↗</Link>{str(s.status)==='DRAFT'&&<CommandForm command="publish_service" label="Publish" values={{service_id:str(s.id)}} returnTo={route}/>} {str(s.status)==='PUBLISHED'&&<CommandForm command="pause_service" label="Pause" values={{service_id:str(s.id)}} returnTo={route}/>}</div></div>)}</div>:<Empty title="No services yet"><Link href="/creator/services/new" className="text-link">Create your first service →</Link></Empty>}</main>;
}
if(route==='/buyer/orders'){
  const d=row(await getDashboardData(actor));
  return <main className="container">{notices}<PageHeading eyebrow="Buyer workspace" title="Orders" description="Fund, review, and keep every project decision in one private workspace."/><OrderList orders={d.orders}/></main>;
}
if(route==='/buyer/requests'||route==='/creator/requests'){
  const d=row(await getDashboardData(actor));
  return <main className="container">{notices}<div className="section-heading"><PageHeading eyebrow="Briefs" title="Open requests" description="Requests and applications stay scoped to the people involved."/><Link className="button button-dark" href="/buyer/requests/new">Post a brief ↗</Link></div>{rows(d.requests).length?<div className="cards">{rows(d.requests).map(r=><RequestCard key={str(r.id)} item={r}/>)}</div>:<Empty title="No requests yet"><Link href="/buyer/requests/new" className="text-link">Share a project brief →</Link></Empty>}</main>;
}
if(route==='/buyer/requests/new')return <main className="container">{notices}<PageHeading eyebrow="Buyer setup" title="Share the project behind the ask." description="Give creators enough context to respond with a useful approach and quote."/><div className="panel"><CommandForm command="create_request" label="Publish brief" returnTo="/requests"><Field name="title" label="Brief title" required placeholder="Three launch videos for our new product"/><CategoryField/><Field name="brief" label="Brief" type="textarea" required placeholder="Audience, goals, deliverables, references, and constraints."/><div className="form-grid"><Field name="budget" label="Budget (USD)" type="number" required placeholder="1200"/><Field name="per_creator_cap" label="Per creator cap (USD)" type="number" placeholder="1200"/><Field name="target_hires" label="Creators needed" type="number" value="1" required/><Field name="deadline" label="Deadline" type="datetime-local" required/></div></CommandForm></div></main>;
if(route==='/creator/auctions/new'){
  const d=row(await getDashboardData(actor));
  const services=rows(d.services).filter(s=>str(s.status)==='PUBLISHED');
  return <main className="container">{notices}<PageHeading eyebrow="Optional discovery" title="Auction a specific slot." description="An auction reserves one service capacity unit and creates an order only after a winner funds it."/><div className="panel"><CommandForm command="create_auction" label="Schedule auction" returnTo="/auctions"><Field name="service_id" label="Published service" required><select name="service_id" defaultValue=""><option value="" disabled>Select a service</option>{services.map(s=><option key={str(s.id)} value={str(s.id)}>{str(s.title)} · {money(s.price_minor)}</option>)}</select></Field><div className="form-grid"><Field name="starting_price" label="Starting price (USD)" type="number" required/><Field name="minimum_increment" label="Minimum increment (USD)" type="number" value="25" required/><Field name="buy_now_price" label="Buy now price (optional)" type="number"/><Field name="starts_at" label="Starts at" type="datetime-local" required/><Field name="ends_at" label="Ends at" type="datetime-local" required/></div></CommandForm></div></main>;
}
if(route==='/settings/profile'){
  const d=row(await getDashboardData(actor));
  const profile=row(d.profile);
  return <main className="container">{notices}<PageHeading eyebrow="Account" title="Your public profile" description="A clear profile helps buyers choose the right creator and keeps roles server-controlled."/><div className="panel"><CommandForm command="update_profile" label="Save profile" returnTo="/settings/profile"><div className="form-grid"><Field name="display_name" label="Display name" value={str(profile.display_name,actor.display_name)} required/><Field name="handle" label="Public handle" value={str(profile.handle)} required/><Field name="niche" label="Niche" value={str(profile.niche)} placeholder="Independent creator"/><Field name="social_url" label="Social or portfolio URL" value={str(profile.social_url)} placeholder="https://…"/></div><Field name="bio" label="Bio" type="textarea" value={str(profile.bio)} required placeholder="A short description of your experience and working style."/></CommandForm></div></main>;
}
return <main className="container">{notices}<Empty title="That page is not available"><Link href="/dashboard" className="button button-dark">Return to workspace ↗</Link></Empty></main>;
}
