# Auction ended; no winner

Status: local job behavior **verified local-db+mock** at `90004fd` ([W4-A](../evidence/claude-W4-A.md), AUC-05/06). End-to-end incident rehearsal **NOT_RUN**. P3 done-local; P4 in progress (Claude). [Shared tools and limits](README.md) apply. Platform fee always **0%**.

Owner: engineering/operator for incidents; Claude coordinates integration. MEDIUM, HIGH for duplicate winner/order or lost claim.

1. Read `/auctions/{id}` and `GET /api/auctions/{id}/snapshot`. The snapshot includes server time, version, accepted bid history and viewer standing. Compare `app.auctions`, `app.bids`, `app.auction_purchase_intents`, `app.workload_claims` and related `app.orders`; screenshots and browser time are not winner evidence. Use `/admin/orders/{orderId}` and `/admin/operations` for an existing payment obligation.
2. `close_due_auctions` (`closeDueAuctions` in `runJobsOnce`) starts due SCHEDULED auctions and closes due auctions under a row lock. W4-A AUC-05 proves repeated/concurrent no-bid close → **NO_BIDS**, one claim release and zero reserved units. AUC-06 proves concurrent jobs plus seller close → one intent and one winner order, with platform fee zero and the same 24 h deadline on payment and hold.
3. Seller `close_auction` through `POST /api/commands` takes `auction_id` and the standard idempotency envelope. It refuses early close (422) or an already closed auction (409); job replay is idempotent. A winning close enters AWAITING_WINNER_PAYMENT. Verified funding → **SETTLED**. Unpaid winner expiry → **WINNER_DEFAULTED**, intent DEFAULTED and released capacity (AUC-10). Expired Buy Now uses a 15 min hold, ends WINNER_DEFAULTED with intent EXPIRED, and never reopens (AUC-11). No runner-up is automatically charged.
4. Before any valid bid, the seller can use `cancel_auction` with `auction_id` and optional `reason`; a concurrent first bid serializes against cancellation (AUC-12). Moderator/admin `admin_invalidate_bid` takes `bid_id` and a required audit `reason`, only while open. It retains the history/audit row and recomputes the leader without resetting `first_valid_bid_at` or re-enabling Buy Now (AUC-08). These commands do not override a completed sale.
5. In Claude's isolated local mock process, use the [documented job hook](README.md) and inspect every report, including `close_due_auctions`. The hook runs all jobs and has no auction selector. Preserve the existing reservation on order handoff; never allocate a second unit. Keep the original Next process because mock provider history is in memory.

Verify one canonical winner/order/claim, or the evidenced terminal no-sale outcome. Late funding must not resurrect a defaulted order/auction or reclaim released capacity; AUC-14 records one LATE_FUNDING case. Follow [runbook 01](01-buyer-charged-order-not-funded.md) for provider/case recovery.

Limits: seller payout readiness at scheduling is not checked; P3-07 bidder/uplift metrics are not implemented. C5 E2E is authored but not executed. Deployed auction scheduling and alerting are **NOT IMPLEMENTED**; local job evidence does not establish a deployed scheduler.

Never force paid/refunded/released state, write balances or capacity counters, delete audit evidence, or retry an uncertain financial effect with a new operation key. Record actor, UTC time, original identifiers, reason, observed result and next owner. No live payment, external email or deployment is authorized here.
