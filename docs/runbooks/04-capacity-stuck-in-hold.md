# Capacity stuck in hold

Status: local procedure documented; end-to-end incident rehearsal **NOT_RUN**. Procedure baseline `3bddff9`; P3 auction/job update verified at `90004fd` (see the index); active-order limit update from W7-CAP (uncommitted, after `9c5b157`). Concurrent P4 changes are not verified here. [Shared tools and limits](README.md) apply. Platform fee always **0%**.

Owner: engineering + finance; MEDIUM, HIGH for counter mismatch or possible oversell.

1. Read `app.workload_claims` (creator_id, origin, order_id, auction_id, state, units, expires_at) and `app.creator_workloads` (max_active_units, accepting_orders, held_units, active_units). Held units must equal HELD+EXPIRY_RECONCILING claims and active units ACTIVE claims; `app.workload_counter_drift` lists any creator where they differ, and `/admin/operations` shows it as "Workload counter drift". Triggers derive the counters; the claim insert trigger refuses a new claim above the limit or while the creator is paused.
2. Follow the order on `/admin/orders/[orderId]` and uncertainty on `/admin/operations` / `/admin/cases`. Reconcile original provider operations before considering expiry; UNKNOWN or captured payments keep the claim in EXPIRY_RECONCILING.
3. In the isolated original Next process, the shared local hook runs `reconcile_provider_operations` before `expire_checkout_holds`. Expiry cancels a still-open provider intent before release, and rechecks state under locks. A terminal unpaid checkout cancels the order, and the order status trigger releases the claim once. Approval (DONE) and cancellation/refund (RELEASED) also free units through that trigger; nothing else should.
4. Lock contract: **aggregate (request/auction) → creator workload → order** ([W7-CAP](../evidence/claude-W7-CAP.md)). Claimers lock the workload and insert new orders; order-first workers lock an existing order and reach the workload only through claim triggers, so no cycle forms. Dedicated funding/expiry race coverage remains a follow-up; do not repair counters manually.
5. For request hires inspect `app.hire_offers`, `app.request_budget_reservations` and `/requests/[id]`. OFFERED expires after at most 24h via `expire_hire_offers`; ACCEPTED hands over to checkout expiry. The order trigger commits budget on FUNDED, releases on unpaid cancellation/lapse or full REFUNDED. Partial refunds stay committed. Pre-0007 requests have no reservation backfill; do not trust their zero counters as complete historical obligations.
6. Auctions hold one claim (origin AUCTION) from scheduling. Close or Buy Now moves the same claim to the sale order; no bids or seller cancellation release it.
7. On a drift row (`check_workload_counters` opens one HIGH `WORKLOAD_COUNTER_DRIFT` case): ask the creator to pause new orders or disable `CHECKOUT_CREATION_ENABLED` on `/admin/flags` with a reason, which preserves webhook/refund/reconciliation. Find the bug before resuming. Automatic repair is **NOT IMPLEMENTED**.

Verify `workload_counter_drift` is empty, no order has two claims, and request budget/count conservation holds. A creator may show more in-flight units than their limit after lowering it; that is expected and only blocks new claims. No per-slot ACCESS model is accepted yet.

Never force paid/refunded/released state, write balances or capacity counters, delete audit evidence, or retry an uncertain financial effect with a new operation key. Record actor, UTC time, original identifiers, reason, observed result and next owner. No live payment, external email or deployment is authorized here.
