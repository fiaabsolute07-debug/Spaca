{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm env:check ${APP_ENV:-production} && pnpm build",
  "regions": [
    "hnd1"
  ],
  "crons": [
    {
      "path": "/api/cron/jobs",
      "schedule": "0 3 * * *"
    }
  ]
}
